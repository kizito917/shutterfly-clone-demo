// // External imports
// const express = require('express');

// // Internal imports
// const { canvaRedirect, canvaAuthSuccess, canvaAuthFailure, canvaAuthorization, revokeCanvasAuthorization } = require('../controllers/canvaAuth.controller');

// const router = express.Router();

// router.get('/oauth/redirect', canvaRedirect);

// router.get('/success', canvaAuthSuccess);

// router.get('/failure', canvaAuthFailure);

// router.get('/authorize', canvaAuthorization);

// router.get('/revoke', revokeCanvasAuthorization);

// module.exports = router;